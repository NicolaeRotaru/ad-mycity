# ===========================================
#  CHIAVE SEGRETA — riempi e rinomina in .env.local
#  Questo file resta SOLO sul tuo PC. Mai online.
# ===========================================

# --- ANTHROPIC (il cervello AI) ---  [OBBLIGATORIO]
# La stessa chiave che usi gia nel sito MyCity.
# La trovi in: console.anthropic.com -> Settings -> API Keys
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxx

# --- GITHUB (per ANALIZZARE il marketplace mycity) ---  [OPZIONALE]
# Serve solo per far leggere all'assistente il codice del sito (SOLA LETTURA).
# Senza queste variabili l'assistente funziona lo stesso (chat + ricerca web).
# Token "fine-grained" con permesso di sola lettura su "Contents" del repo mycity.
GITHUB_TOKEN=github_pat_xxxxxxxx
GITHUB_OWNER=NicolaeRotaru
GITHUB_REPO=mycity
GITHUB_DEFAULT_BRANCH=main

# --- MEMORIA (per restare "vivo": salva i giri autonomi) ---  [OPZIONALE]
# Senza, l'assistente gira lo stesso ma non ricorda i giri tra una volta e l'altra.
# Crea un progetto su supabase.com e una tabella "briefings" (vedi LEGGIMI.md).
SUPABASE_URL=https://xxxx.supabase.co
SUPABASE_SERVICE_KEY=eyJxxxxxxxx

# --- BATTITO AUTONOMO (il cron che lo sveglia ogni ora) ---  [CONSIGLIATO online]
# Una parola segreta a tua scelta: protegge l'endpoint del battito.
# In test puoi lasciarla vuota e usare il pulsante "Aggiorna ora".
CRON_SECRET=
