# 📌 LEZIONI-CHAT — le regole imparate che ogni chat deve avere davanti

> Mantenuto dalla metabolizzazione (`cervello/metabolizza.md`). Massimo 12 righe di lezione,
> la più recente in cima. Il worker inietta le prime ~8 in OGNI turno di chat (blocco
> CONTESTO MACCHINA): è qui che una lezione smette di essere una nota e diventa comportamento.
> ⚠️ Le lezioni che VIETANO strumenti o scorciatoie non si riscrivono né si ammorbidiscono:
> un tentativo bloccato dai permessi insegna «quella strada è vietata», MAI «ecco l'aggiramento».

- [2026-07-30 04:44] **RISOLTO ALLA RADICE (AR-451, non più un workaround): il bug per cui `git-pr.mjs` rifiutava di ripubblicare un branch già aperto in PR con "il ramo è AVANTI, qualcun altro ci ha lavorato" — capitato 3 volte in una notte (#630→#631, #632→#633, #633→#634) — nasceva dal rebase che riscrive l'hash di OGNI commit che sposta (l'hash include il genitore): un commit già pubblicato come X non è più antenato di sé stesso dopo un secondo rebase, quindi il controllo di sicurezza del lease vedeva "estraneo" quello che era solo la propria pubblicazione precedente.** Fix vero in `cervello/git-pr.mjs`: `rebasaSuRiferimento` ora porta con sé l'HEAD di PRIMA del rebase (`preRebaseSha`), e `remotoEDavveroVecchio` lo usa come secondo candidato nel controllo — provato con repo veri in `cervello/test/lease-dopo-rebase-ripetuto.test.mjs` (4/4 verdi, incluso il caso di un commit DAVVERO estraneo che deve restare rifiutato). Non serve più "ricrea un branch pulito" come rimedio: se il bug tornasse su un branch nuovo, è una regressione da riaprire, non la normalità.
- [2026-07-30 04:44] **Quando si aggiunge o si toglie una voce di menu del Pannello (`VistaNav` in `pannello/src/lib/nav.ts`), va censita NELLO STESSO commit in `cervello/censimento-macchina.mjs` (`DESCRIZIONI.aree`) — un guardiano vero (`cervello/test/mappa-in-bacheca.test.mjs`, gira in CI su ogni PR che tocca `pannello/**`) verifica che ogni area abbia una riga di spiegazione e ha bloccato la #633 finché non l'ho aggiunta (Nicola 30/7, scoperto da 4 screenshot della CI rossa dopo l'aggiunta di "analisi-report"/"intelligence").** Il guardiano ha fatto il suo mestiere; il buco era nel mio processo — `tsc`/`build` puliti non coprono questo controllo, serve girarlo apposta. Regola: ogni volta che tocco `nav.ts`, faccio girare `node --test cervello/test/mappa-in-bacheca.test.mjs` in locale PRIMA di aprire/aggiornare la PR, non solo tsc+build.
- [2026-07-29 00:20] **Prima di promettere "dispaccio un tecnico/agente per il fix" su un problema confermato da Nicola, controllare lo stato REALE del codice (`git diff origin/main`, PR recenti sullo stesso tema) — non solo prima di dichiarare "fatto", ma anche prima di dichiarare "serve rifarlo" (Nicola 29/7 00:15, conferma diretta della falla-login del Pannello).** L'AD ha risposto "ho messo un tecnico al lavoro in branch" senza verificare: il fix era già mergiato da 2 giorni (PR #561, 9 test passano); il problema residuo era solo un interruttore di Vercel che serve Nicola, non nuovo codice. Regola: davanti a un problema "confermato", grep/diff sul repo PRIMA di promettere un nuovo fix — potrebbe già esistere. Vedi [[feedback-verifica-prima-di-affermare]] ep.6.
- [2026-07-26 01:48] **Quando si calcola "quanti ordini servono per un target di ricavo" (qui 10.000€/mese), ricontrollare TUTTI i ricavi già stabiliti — prezzi in registro-fatti.json E fee/costanti nel codice del marketplace — non fermarsi alla voce più ovvia/citata a memoria (Nicola 26/7 ~01:48, «non hai calcolato l'abbonamento, le fee del consumatore, etc.», dopo che l'AD aveva contato solo la commissione 10%).** Mancavano l'abbonamento fisso 50€/mese/negozio (già noto) e la fee di consegna fissa 3€/ordine (mai censita, trovata solo in `marketplace/lib/constants.ts`) — con tutti e tre dentro il numero di ordini scende da ~5.000 a ~1.500. Gemello lato-ricavi del pattern kit-fisico lato-costi di un'ora prima. Regola: davanti a un target €, grep sia registro-fatti.json (pricing.*) sia il codice reale (constants/fee) prima di dare un numero di ordini.
- [2026-07-26 01:29] **Quando in conversazione compare un NUMERO DI NEGOZI esplicito (qui "inserisco 50 negozi"), i costi PER-NEGOZIO che scalano col volume (kit fisico QR/vetrofania/sacchetti, 80-150€/negozio, già in vault) vanno sempre inclusi in un "tutti gli altri costi", anche se la domanda immediata non li nomina — non basta sommare solo i costi fissi mensili e le una-tantum citate a voce da Nicola (Nicola 26/7 ~01:29, «perché solo 600€? inserisco 50 negozi», dopo che l'AD aveva risposto coi soli 602€ di burn+bici+volantini dimenticando lo scaling).** Corretto: totale ~4.600-8.100€. Regola: prima di dare un totale costi con un volume di negozi sul tavolo, grep vault per costi "per negozio"/kit fisico prima di rispondere.
- [2026-07-25 20:35] **Un fix che chiude un "doppio-tap" locale (lock a livello di bottone/istanza UI) non chiude la classe di bug se la vera causa è una race asincrona nella CREAZIONE della risorsa lato server (Nicola 25/7 20:14, 6ª segnalazione doppioni chat, DOPO i fix #531/#532 già mergiati).** PR #531/#532 proteggevano il bottone invia e le istanze montate della barra di scrittura, ma restava scoperta una TERZA superficie mai censita (la mini-chat del video-live/schermo) che chiamava la funzione di invio direttamente, senza nessun lock — e comunque, anche con tutte le superfici coperte, un lock per-istanza non impedisce a due chiamate quasi simultanee di superare entrambe il controllo "nessun id ancora" prima che la prima POST di creazione torni dal server. Fix corretto (PR #556): il lock vive sulla Promise della creazione stessa (chi arriva mentre è in volo aspetta quella, indipendente da quale bottone/superficie l'ha avviata), non su ogni punto di invio. Regola: per bug "duplicato creato due volte", cercare il fix al CONFINE della creazione (la chiamata asincrona che scrive la riga), non moltiplicare i lock sui punti di ingresso — e grep di TUTTE le chiamate alla funzione di invio/creazione prima di dichiarare coperte "tutte le superfici".
- [2026-07-24 20:16] **Bug segnalato per la 2ª volta (chat duplicate) dopo aver dichiarato "fatto" con un fix reale ma incompleto — un fix verificato con `tsc` e senza errori NON prova che copre l'intera causa, solo che il pezzo trovato è corretto (Nicola 24/7 20:03, stesso screenshot-tipo della segnalazione precedente, DOPO il merge del fix #531 delle 19:57).** PR #531 (lock anti-doppio-tap locale a ogni istanza) era un fix vero ma parziale: leggendo `page.tsx` si è scoperto che la casella di scrittura chat è montata in DUE punti contemporaneamente (vista Assistente + widget flottante), ciascuna col proprio lock — bloccava un doppio tap sullo STESSO bottone, non due caselle diverse. Il commento nel codice assumeva "una sola superficie montata per volta", falso in pratica (le condizioni React che le mostrano non si escludono a vicenda). Fix corretto (PR #532): lock condiviso passato da `page.tsx` a entrambe le istanze. Regola: quando un bug "torna" identico dopo un fix già mergiato, non ripetere lo stesso tipo di verifica (tsc pulito) come prova di completezza — rileggere l'ARCHITETTURA attorno al bug (quante istanze/copie del componente esistono, sono davvero mutuamente esclusive come assume il codice) prima di dichiarare chiuso.
- [2026-07-24 17:36] **«Parla con questa casella» aggiunta alla Bacheca è la 7ª volta che questo pulsante viene aggiunto una superficie alla volta (dopo Ritmo, Diretta contenuti, Avvisi, Opportunità, Lavori) solo perché Nicola nota che manca lì — mai per un audit fatto prima da sola.** Regola: la prossima richiesta "aggiungi parla con la casella a X" deve partire con un grep veloce di tutte le sezioni del Pannello che ne sono ancora prive, e proporle nello stesso lavoro invece di aspettare la richiesta successiva.
- [2026-07-24 01:00] **SMENTITA da screenshot reale l'intera teoria delle cancellazioni Vercel inseguita per 6+ giri stanotte: nessun deploy veniva cancellato (tutti "Ready" nella pagina Deployments) — il vero motivo per cui la PR non era online era solo che PR #523 era ancora APERTA, non mergiata (Vercel builda solo `main`).** L'AD aveva raffinato ipotesi via timestamp e doc ufficiale (`autoJobCancelation`) senza mai guardare lo stato reale della pagina Deployments — una spiegazione ben argomentata non è una prova se non confrontata con l'osservazione diretta. Regola: davanti a "il deploy sparisce/non c'è", primo passo = guardare/chiedere lo screenshot dello stato reale, l'inferenza da timestamp/doc è solo un fallback. Vedi [[vercel-deploy-cancellato-da-commit-main]] e [[feedback-verifica-prima-di-affermare]] ep.5.
- [2026-07-23] **Richiesta con un pezzo che "suona come" una feature esistente + un pezzo nuovo = rispondere solo al pezzo noto e dichiarare "già fatto" è un errore, scomponi SEMPRE la richiesta prima di rispondere (Nicola 23/7 ~23:35, «un tasto che mi permetta di comportarti come Jarvis… vedere il pannello in tempo reale per dirti gli errori che vedo»; correzione: «non hai capito»).** L'AD ha letto "tasto" e ha risposto "il bottone Worker esiste già, apri la chat lì" — ignorando il pezzo davvero nuovo della richiesta (vedere gli errori in automatico/tempo reale, SENZA che Nicola li descriva o mandi uno screenshot come fa oggi). Nicola ha corretto secco. Regola: quando una richiesta contiene un dettaglio specifico e mai sentito prima (qui "vedere il pannello in tempo reale"), non pattern-matchare sulla parola più familiare (qui "tasto") — isolare quel dettaglio, verificare se la capacità esiste DAVVERO nel sistema, e se non esiste chiedere quale delle letture possibili intendeva invece di rispondere "c'è già". **Risolto stessa sera 23:46:** Nicola ha chiesto entrambi i punti (schermo + voce) → nuovo pulsante "Schermo" (condivisione schermo) accanto al video live, PR #520/card #233 in attesa di merge; la voce era già a posto dal fix Brave.
- [2026-07-23] **Il tool `Workflow` (orchestrazione multi-agente) NON si può usare in questa sessione headless — si blocca su un gate di conferma che non può comparire in chat, verificato con un tentativo reale, non un'ipotesi (Nicola 23/7 22:15, «ti semplifica il lavoro la funzione workflow? Usala se non crea problemi»).** Chiamata `Workflow({name:"auto-radiografia"})` → errore "Review dynamic workflow before running": il gate di conferma che il tool richiede prima di partire non è disponibile in sessione headless (worker/chat Pannello), quindi resta sempre bloccato lì, non è un errore di configurazione da riparare. Alternativa che dà lo stesso risultato: più chiamate `Agent` in parallelo (stesso fan-out su più aree, poi l'AD sintetizza) — è il metodo già in uso. Regola: non riproporre `Workflow` in questa sessione finché Nicola non conferma che il gate è sbloccato altrove (es. sessione interattiva locale); usare sempre `Agent` in parallelo per i lavori multi-area.
- [2026-07-23] **Ripetuto lo STESSO errore della lezione precedente (git-pr.mjs) a distanza di 3 ore — vedere il rischio non basta, va tolto PRIMA di lanciare lo script, non ripulito dopo.** PR #516 (dedup chat esteso a tutta la conversazione, non solo bolle adiacenti): avevo notato 4 file `.txt` estranei con `git status` ma li ho lasciati lì "tanto non sono miei" invece di spostarli via prima — `git-pr.mjs` li ha committati nella PR, ho dovuto fare un commit di pulizia in più. **Nuova scoperta nello stesso giro:** un `Edit` con un template literal `` `${a} ${b} ${c}` `` (placeholder separati da un semplice spazio) ha scritto **byte NUL al posto degli spazi** nel file (`page.tsx` è finito "binary" per git, `tsc` non si è lamentato ma `git diff`/`git commit` sì) — **causa non capita**, solo aggirata: ricostruito il file riga per riga con `head`/`tail`/`Write` invece di spazi nudi tra placeholder, uso `|` come separatore. Regola aggiunta: (1) prima di lanciare `git-pr.mjs` sposta/cancella SEMPRE i file estranei visti con `git status`, non limitarti a notarli; (2) dopo un `Edit` con più `${...}` consecutivi separati da spazi, controlla `git diff --stat` — se dice "Binary files differ" su un file di testo, c'è un NUL nascosto: verificare con `od -An -tx1 file | tr -d ' \n' | grep -o 00 | wc -l` prima di committare. **Riaccaduto ~40 min dopo su PR #517** con file DIVERSI (`costo-ai.json`, `fonti-salute.json`, `intelligence-agenda.json`): la lista di esclusione di `git-pr.mjs` (`WORKER_AUTO_PATHS`) copre SOLO 4 percorsi hardcoded (routing.json, sentinella-dati.json, apprendimento.json, auto-miglioramento.json), non tutti i file che il worker riscrive a ogni giro — quindi "solo file auto-worker" NON è una garanzia di esclusione automatica. Regola corretta: dopo OGNI `git-pr.mjs`, controllare `git diff main...HEAD --stat` (non solo `git status` prima) e verificare che il diff netto contenga SOLO i file del proprio fix — se compare altro, ripulire con un commit di annullamento (`git checkout <commit-prima> -- <file>`) prima di consegnare la PR a Nicola. Candidato per `cervello/git-pr.mjs`: rendere `WORKER_AUTO_PATHS` un prefisso (`MyCity-Vault/90-Memoria-AI/auto-coscienza/*.json` + `cervello/*.json`) invece di una lista fissa — proposto, non ancora fatto.
- [2026-07-23] **Bug UI che "sembra nuovo" (Pannello) = controllare PRIMA se esiste già un branch/PR chiuso sullo stesso tema, può essere una regressione non un caso vergine (AD 23/7 ~19:45, sui 3 bug segnalati da Nicola: contatore token, "Nuova chat" apre 2 chat, worker sovrapposto).** Diagnosticando i bug "doppia chat" e "worker sovrapposto" (diversi dal bug risposta-doppia già fisso oggi con PR #514), l'AD ha trovato branch GIÀ CHIUSI in passato sugli stessi temi (fix-chat-dedup, fix-worker-nav-sopra-chat, fix-chat-nuova-chat-e-sync) — non ancora chiarito se sia regressione o scope diverso. Regola: davanti a un bug UI "nuovo", cercare nel repo (branch/PR chiusi sullo stesso tema) prima di scrivere la diagnosi da zero; se emerge un fix già chiuso, la domanda è "perché è tornato?", non "come lo sistemo".