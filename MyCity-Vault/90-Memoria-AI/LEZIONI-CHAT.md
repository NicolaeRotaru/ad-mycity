# 📌 LEZIONI-CHAT — le regole imparate che ogni chat deve avere davanti

> Mantenuto dalla metabolizzazione (`cervello/metabolizza.md`). Massimo 12 righe di lezione,
> la più recente in cima. Il worker inietta le prime ~8 in OGNI turno di chat (blocco
> CONTESTO MACCHINA): è qui che una lezione smette di essere una nota e diventa comportamento.
> ⚠️ Le lezioni che VIETANO strumenti o scorciatoie non si riscrivono né si ammorbidiscono:
> un tentativo bloccato dai permessi insegna «quella strada è vietata», MAI «ecco l'aggiramento».

- [2026-07-18] **`git-pr.mjs` è bloccato dalla chat perché fa un push a GitHub con il token segreto — blocco di sicurezza permanente, non un bug (18/7).** Nicola ha chiesto «Perché non riesci ad aprire la PR?» tre volte nella stessa sessione. Risposta corretta: dai SUBITO il comando VPS (`node cervello/git-pr.mjs --repo ad-mycity --base main`) + accoda in AZIONI-IN-ATTESA. Non limitarti a «il tool è bloccato» — dai la soluzione. Il blocco NON si aggira dalla chat: NON prometterlo.
- [2026-07-18] **Prima di dire «lo devi fare tu dal VPS», PROVA il comando — poi delega se fallisce (18/7).** L'AD aveva detto a Nicola di eseguire 2 `git -C marketplace` senza averli mai tentati; Nicola ha dovuto chiedere «Non riesci tu?» e solo allora l'AD ha provato, scoprendo che sono bloccati dall'allowlist. Regola: tenta il comando, poi riporta l'esito reale. Fatto confermato: `git -C /opt/mycity/ad-mycity/marketplace` (operazioni sulla sottocartella marketplace/) è bloccato — solo `ad-mycity/` root è in allowlist.
- [2026-07-18] **Quando Nicola chiede «è già fatto?» o «non avevo già risolto X?», rispondere con lista 3-tier obbligatoria (18/7).** Nicola ha chiesto «Non avevo già fatto i fix di tutti i bloccanti?» perché non aveva visibilità su cosa era completato vs in attesa firma vs non iniziato. Risposta generica «in parte sì, in parte no» non basta. Formato obbligatorio: ✅ FATTO [PR/commit] · ⏳ IN ATTESA FIRMA [#card] · ❌ NON INIZIATO [lista]. In ogni giro con fix multi-sessione in corso, includere un mini-dashboard bloccanti.
- [2026-07-18] **Prima di aprire una PR per aggiungere una funzione, verifica che non sia già su main** — PR #449 (housekeeping coda) aveva conflitti perché la riga utile era già su main (commit `8abf5197`). Housekeeping automatico già operativo. PR #449 va chiusa senza merge. Regola: `git log --oneline main | grep <funzione>` prima di creare branch.
- [2026-07-18] **Se allowlist non funziona dopo «C'è già» di Nicola, il JSON di `settings.local.json` è malformato — verifica la sintassi (virgola mancante = voce ignorata silenziosamente, 18/7). ⚠️ Anche dopo il fix della virgola, i permessi NON cambiano nella sessione corrente: l'allowlist viene caricata UNA VOLTA all'avvio della chat. La nuova voce entra in vigore solo alla PROSSIMA sessione.** L'entry `"Bash(node cervello/git-pr.mjs:*)"` era presente ma mancava la virgola → JSON invalido → allowlist ignorata. Nicola ha sistemato la virgola ma il blocco persisteva (sessione già avviata). Fix per la sessione corrente: sempre VPS (`node cervello/git-pr.mjs --repo ad-mycity --base main`). Regola: dopo «C'è già + fix virgola», di' subito «serve nuova sessione» e dai il comando VPS alternativo — non riprovare il tool bloccato.
- [2026-07-18] **Correzione Nicola (18/7): ristoranti e osterie NON sono nel target MyCity** — anche se già su Deliveroo, ben valutati su Google o con alta commissione da togliere. Tigellabella (tigelleria/osteria) era in coda come prospect ma Nicola ha escluso la categoria. Regola stabile: nel prospecting filtro via tutto il settore ristorazione classica (ristoranti, osterie, trattorie) prima di proporre.
- [2026-07-18] **Leggi STATO.md PRIMA di chiedere a Nicola un URL o un dato env** — `MARKETPLACE_SITE_URL=https://mycity-marketplace.com` era in STATO.md dal 18/7 04:30 ma l'AD l'ha chiesto 3 volte. Il permesso verbale («ti do il permesso di leggere l'env») NON sblocca `cervello/vps/.env` — blocco di sistema. La risposta è SEMPRE la memoria.
- [2026-07-18] **Correzione Nicola (18/7): «🚫 Messaggio annullato» NON va mandato se sovrascrive una risposta già visibile.** Se il worker/AD si ferma a metà, il messaggio precedente deve restare dov'è. «🚫 Messaggio annullato» / «💭 Sto elaborando…» sono accettabili solo se non c'era ancora nulla di visibile. Se c'era già una risposta → resta in silenzio o aggiungi dopo, MAI sovrascrivere sopra. La regola: ciò che Nicola ha già letto non si cancella.
- [2026-07-18] **File `.claude/*.json` sono bloccati da OGNI modifica via chat — anche skill `update-config` e Write tool falliscono (18/7):** quando Nicola chiede «aggiungi X all'allowlist» o «sblocca Y», l'AD non può farlo da sé. Risposta corretta: «Non posso modificare `.claude/` dalla chat — dal terminale VPS: `nano /opt/mycity/ad-mycity/.claude/settings.local.json` e aggiungi `"Bash(node cervello/nome-script.mjs:*)"` nell'array allowedTools.» Non promettere «ci penso io»: il blocco è un meccanismo di sicurezza, non un bug.
- [2026-07-18] **Verifica il branch PRIMA di ogni Edit — se `git branch --show-current` mostra `main`, fai checkout prima (18/7 ricorrente):** i commit finiscono su main per errore se l'AD dimentica di spostarsi sul branch dopo averlo creato. Sintomo: «sono finite su main anche questa volta». Lo spostamento richiede `git cherry-pick` (NON in allowlist dalla chat) → blocco. Sequenza obbligatoria: `git checkout -b <branch>` → verifica con `git branch --show-current` → Edit → commit immediato. Mai dare per scontato di essere sul branch giusto.
- [2026-07-18] **`watch-main` fa `git reset --hard origin/main` ogni 5 min — qualsiasi Edit senza commit immediato è PERSA (18/7):** la sequenza obbligatoria è: crea branch → Edit file → `git add` + `git commit` SUBITO (nessuna pausa tra Edit e commit). Se il watch-main gira nel mezzo, le modifiche spariscono senza avviso. Sintomo: «le modifiche ci sono» ma `git diff HEAD` è vuoto. Fix: riapplicare l'Edit sul branch e committare immediatamente.
- [2026-07-18] **Caselle Pannello: linguaggio umano + nome file specifico + impatto (Nicola 17-18/7):** ogni avviso deve fare TRE cose: (1) frase in italiano chiara (no gergo `rc=1`/`conflitti/0-byte`), (2) nominare il DATO/FILE specifico rotto («il file STATO.md aveva un conflitto» non «c'è qualcosa di sporco»), (3) spiegare l'impatto concreto («se fosse uscito così, il Pannello avrebbe mostrato dati mescolati e illeggibili»). Gergo tecnico in accordion collassabile. PR #439 ha coperto solo (1); il follow-up Nicola 18/7 richiede (2)+(3) nello script che genera l'avviso.
- [2026-07-17] ⭐ PERMANENTE (Nicola: «impara questa lezione e ricordatela per sempre») **Il file `.env` del worker è `cervello/vps/.env` — NON `/opt/mycity/ad-mycity/.env` (verificato 17/7 su mycity-worker-chat.service):** il file systemd ha `EnvironmentFile=/opt/mycity/ad-mycity/cervello/vps/.env`. Il file root `.env` è vuoto/assente. Ogni nuova variabile d'ambiente (token, chiave API) va in `cervello/vps/.env` + poi `sudo systemctl restart mycity-worker-chat.service`. MAI mandare Nicola a modificare `/opt/mycity/ad-mycity/.env` — è il file sbagliato. MAI cercarlo nel codice del worker — è una variabile d'ambiente del processo.

