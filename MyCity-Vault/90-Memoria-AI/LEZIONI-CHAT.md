# 📌 LEZIONI-CHAT — le regole imparate che ogni chat deve avere davanti

> Mantenuto dalla metabolizzazione (`cervello/metabolizza.md`). Massimo 12 righe di lezione,
> la più recente in cima. Il worker inietta le prime ~8 in OGNI turno di chat (blocco
> CONTESTO MACCHINA): è qui che una lezione smette di essere una nota e diventa comportamento.
> ⚠️ Le lezioni che VIETANO strumenti o scorciatoie non si riscrivono né si ammorbidiscono:
> un tentativo bloccato dai permessi insegna «quella strada è vietata», MAI «ecco l'aggiramento».

- [2026-07-17] **Approvazione in chat (testo) = sufficiente — nessun popup necessario (Nicola confermato 17/7):** «non vedo il popup, ma non serve, basta che mi chiedi l'approvazione in chat». Modello operativo: chiedi il permesso in testo, Nicola risponde in testo, è via libera. Il popup non compare mai dal Pannello — smettere di aspettarlo o citarlo come problema.
- [2026-07-17] **PR con conflitti = non è fallita, va risolta — git-pr.mjs funziona (PR #422 aperta):** quando main avanza con altre PR su stesso file (page.tsx), il branch da mergiare entra in conflitto. Non è un push fallito: la PR esiste su GitHub. Risoluzione: rebase da VPS o "Resolve conflicts" su GitHub web (tieni i fix + resto di main). Non riaprire la PR, risolvi quella che c'è.
- [2026-07-17] **Cambio conversazione = scroll al fondo obbligatorio (useEffect su convId)** — quando Nicola apre una chat dalla lista, la pagina mostrava i messaggi dall'inizio costringendolo a scrollare tutto. Fix: useEffect che dipende da convId e chiama scrollAlFondo() — un singolo requestAnimationFrame non basta perché il DOM non è ancora aggiornato al cambio conversazione. PR #420 aperta (branch fix/scroll-conv-bottom, commit e7f15aa0). Pendente: Nicola mergia.
- [2026-07-17] **"Non ricominciare da capo" ≠ "parole man mano" — sono due requisiti DISTINTI (Nicola chiarito esplicitamente 17/7 in 2 messaggi):** "Non ricominciare da capo" = il 2° messaggio NON cancella la risposta in elaborazione → fix della CODA (già committato in fix/chat-coda-messaggi, f8dacc49, PR pendente). "Parole man mano come Claude.ai" = STREAMING → fix SEPARATO nel worker (accodato in #streaming-worker, attende ok Nicola). Non confondere le due: la coda è già fatta, lo streaming no. PR da aprire: `node cervello/git-pr.mjs --repo ad-mycity --base main --accoda`
- [2026-07-17] **Coda messaggi — 3 bug tecnici da gestire assieme (PR #419):** (1) Doppia bolla: prima di creare la bolla risposta finale, verificare che non esista già nel DOM — se sì, non ricrearla. (2) Flicker: `setLoading(true)` non va chiamato se `loading` è già `true` — ogni tick che lo richiama causa un re-render con flash. (3) Bottone mobile: rimuovere `|| loading` dal disabled del pulsante invio quando esiste la coda — il messaggio si accoda, non blocca l'invio. I 3 bug derivano dalla stessa scelta architetturale della coda e vanno gestiti insieme.
- [2026-07-16] **«Metti X insieme a Y» = DUE passi: aggiungi in Y + rimuovi da X — fare solo il rimuovi = pulsanti spariti (Nicola corr. PR #417→#418):** PR #417 ha tolto i pulsanti dall'header senza aggiungerli al tab bar → spariti. PR #418 corretta: (1) aggiungi «Conv»/«Nuova» nella barra dei pulsanti in basso, (2) rimuovi dall'header. Regola: leggere l'istruzione completa — «sposta», «metti insieme», «metti accanto» richiedono SEMPRE sia ADD che REMOVE.
- [2026-07-16] **Regola d'oro del design Pannello (Nicola 16/7): bello all'occhio + efficiente + semplice + minimale** — ogni modifica UI deve passare questo filtro PRIMA del commit. "Bello" = piacevole a guardare. "Efficiente" = meno fatica/passi/ragionamento per Nicola. "Semplice" = una cosa sola, niente rumore. "Minimale" = togli il superfluo, non aggiungere. Se non passa, non si apre la PR.
- [2026-07-16] **Push bloccato = PR non esiste su GitHub: dirlo subito, non accodarla come «pronta»** — se `git-pr.mjs` o `git push` viene bloccato dai permessi, il branch è solo sul VPS. Nicola controlla GitHub e non trova nulla. Regola: se il push non è confermato da un esito reale del comando, scrivere immediatamente «la PR non esiste ancora su GitHub — il codice è solo sul server VPS» e dare il comando manuale. Non marcare come «accodata» una PR che non è mai arrivata su GitHub.
- [2026-07-17] **Streaming chat (richiesta Nicola 17/7): testo deve apparire parola per parola come Claude.ai — fix è nel WORKER, non nel Pannello** — Root confermata: worker manda il blocco completo a fine elaborazione. Fix = DUE modifiche worker: (1) scrivi su DB il testo parziale ogni N secondi mentre Claude ragiona; (2) il frontend già legge questo campo e aggiorna la bolla. Approvazione pendente: Nicola non ha ancora detto «procedi» — l'AD ha spiegato cosa serve e chiesto conferma. Non re-diagnosticare, partire da qui.
- [2026-07-16] **`npx tsc --noEmit` è obbligatorio PRIMA di aprire ogni PR su `pannello/`** — il TS2367 era prevenibile: la PR #411 è stata aperta senza typecheck, il build Vercel ha trovato l'errore dopo il merge e il Pannello è rimasto alla versione vecchia per ore. Regola: modifica → `npx tsc --noEmit` → se fallisce FIX prima → poi commit e PR. Mai aprire una PR su `pannello/` senza il typecheck verde.
- [2026-07-16] **`git push` è in deny-list per design — `node cervello/git-pr.mjs` è il canale approvato** — quando Nicola chiede di aggiungere `git push` all'allowlist, spiegare che `git-pr.mjs` è già in allowlist e gestisce il push internamente. Il blocco precedente era uso del percorso assoluto invece di relativo (cd nella directory corretta e chiamare `node cervello/git-pr.mjs`). Se ci sono file sporchi prima di un checkout, `git stash` → checkout → `git stash pop` (conflitti: tieni HEAD).
- [2026-07-16] **Dopo ogni merge PR Pannello: verifica subito il build Vercel** — il merge su GitHub NON significa "live". Il build Vercel può fallire silenziosamente (es. TS2367) e il sito resta alla versione vecchia. Passaggio obbligatorio: controllare la dashboard Vercel o i log di deploy PRIMA di dire "i fix sono live". Se il build è rosso, trovare l'errore e aprire subito una PR di fix.

