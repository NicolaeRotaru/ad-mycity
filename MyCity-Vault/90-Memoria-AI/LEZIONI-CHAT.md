# 📌 LEZIONI-CHAT — le regole imparate che ogni chat deve avere davanti

> Mantenuto dalla metabolizzazione (`cervello/metabolizza.md`). Massimo 12 righe di lezione,
> la più recente in cima. Il worker inietta le prime ~8 in OGNI turno di chat (blocco
> CONTESTO MACCHINA): è qui che una lezione smette di essere una nota e diventa comportamento.
> ⚠️ Le lezioni che VIETANO strumenti o scorciatoie non si riscrivono né si ammorbidiscono:
> un tentativo bloccato dai permessi insegna «quella strada è vietata», MAI «ecco l'aggiramento».

- [2026-07-18] **Casella Pannello vuota (es. "Esperimento: " senza testo) → primo check: campo mancante nel JSON sorgente** — 18/7 EXP-001 non aveva il campo `ipotesi`; il Pannello lo cercava, non lo trovava, mostrava tutto vuoto senza errore UI. Fix istantaneo: aggiungere il campo nel JSON (🟢). Prima di cercare bug nel codice React, verificare che il dato esista nel file JSON che alimenta la casella.
- [2026-07-18] **Non citare un numero di PR che non hai verificato esiste** — il 18/7 sera l'AD ha detto a Nicola «mergia PR #446» ma quella PR non esisteva (Nicola: «non c'è nessuna PR aperta»). Prima di citare una PR specifica, verifica con `gh pr list --repo NicolaeRotaru/ad-mycity --state open`. Se non sei sicuro, di' «devo controllare» — mai assumere che una PR di sessioni precedenti sia ancora aperta.
- [2026-07-18] **`auto-analisi.json` deve avere ENTRAMBI i campi `voto` E `sintesi` — se manca `sintesi`, il Pannello mostra "Giro in attesa" come se il giro non fosse mai completato (18/7: voto=88 presente, sintesi assente → Pannello bloccato).** Ogni scrittura di `auto-analisi.json` deve includere entrambi i campi obbligatoriamente. Diagnosi rapida: se il Pannello è bloccato su "giro valido in attesa", prima cosa: `grep sintesi cervello/auto-analisi.json`.
- [2026-07-18] **Bug PR description: il file `consegne/tech/pr-ad-mycity-body.md` è condiviso tra TUTTE le PR — durante un rebase, un conflitto su questo file si risolve con il contenuto di main (la vecchia PR) e la nuova PR prende la descrizione sbagliata.** Fix: riscrivere il file PRIMA di ogni PR con il contenuto corretto, oppure passare `--body "..."` inline nel comando. Non lasciarlo accumulare roba vecchia: ogni rebase lo corrompe. La descrizione «fa schifo» segnalata da Nicola (18/7) viene da questo meccanismo.
- [2026-07-18] **Appena apri una PR sostitutiva (pattern #455→#457), chiudi SUBITO la vecchia su GitHub** — PR #455 è rimasta aperta dopo che #457 l'aveva sostituita; Nicola l'ha segnalata come «C'è un conflitto» e ha richiesto intervento. Sequenza completa obbligatoria: crea branch v2 da main → apri nuova PR → chiudi IMMEDIATAMENTE la vecchia (commento «sostituita da #NNN» + close without merge). PR orfane aperte creano confusione e lavoro extra.
- [2026-07-18] **Screenshot allegati nella chat del Pannello → bucket Supabase, NON sul filesystem (18/7): non cercarli con find/ls, non esistono come file.** Nicola ha allegato 6 screenshot di bug — l'AD ha perso tempo a cercarli su disco. Percorso corretto: (a) leggi il codice per inferire i bug visivi, (b) chiedi a Nicola di descrivere verbalmente ciò che vede se il codice non basta. MAI dichiarare «non riesco a trovare i file» senza prima aver letto il codice corrispondente al bug segnalato.
- [2026-07-18] **Quando una PR GitHub ha un conflitto che il rebase non risolve, la strada più veloce è: `git checkout main → git checkout -b fix/nome-v2 → applica fix → commit → apri nuova PR` (pattern #455→#457, 18/7).** Il rebase iterativo in-place su branch con storia divergente spreca tempo. Il nuovo branch da main parte pulito, non porta storia conflittuale. La PR vecchia con conflitto va chiusa/sostituita — non lasciarla aperta con un conflitto irrisolto su GitHub.
- [2026-07-18] **Correzione Nicola (18/7): «🚫 Messaggio annullato» NON va mandato se sovrascrive una risposta già visibile.** Se il worker/AD si ferma a metà, il messaggio precedente deve restare dov'è. «🚫 Messaggio annullato» / «💭 Sto elaborando…» sono accettabili solo se non c'era ancora nulla di visibile. Se c'era già una risposta → resta in silenzio o aggiungi dopo, MAI sovrascrivere sopra. La regola: ciò che Nicola ha già letto non si cancella.
- [2026-07-18] **File `.claude/*.json` sono bloccati da OGNI modifica via chat — anche skill `update-config` e Write tool falliscono (18/7):** quando Nicola chiede «aggiungi X all'allowlist» o «sblocca Y», l'AD non può farlo da sé. Risposta corretta: «Non posso modificare `.claude/` dalla chat — dal terminale VPS: `nano /opt/mycity/ad-mycity/.claude/settings.local.json` e aggiungi `"Bash(node cervello/nome-script.mjs:*)"` nell'array allowedTools.» Non promettere «ci penso io»: il blocco è un meccanismo di sicurezza, non un bug.
- [2026-07-18] **Verifica il branch PRIMA di ogni Edit — se `git branch --show-current` mostra `main`, fai checkout prima (18/7 ricorrente):** i commit finiscono su main per errore se l'AD dimentica di spostarsi sul branch dopo averlo creato. Sintomo: «sono finite su main anche questa volta». Lo spostamento richiede `git cherry-pick` (NON in allowlist dalla chat) → blocco. Sequenza obbligatoria: `git checkout -b <branch>` → verifica con `git branch --show-current` → Edit → commit immediato. Mai dare per scontato di essere sul branch giusto.
- [2026-07-18] **`watch-main` fa `git reset --hard origin/main` ogni 5 min — qualsiasi Edit senza commit immediato è PERSA (18/7):** la sequenza obbligatoria è: crea branch → Edit file → `git add` + `git commit` SUBITO (nessuna pausa tra Edit e commit). Se il watch-main gira nel mezzo, le modifiche spariscono senza avviso. Sintomo: «le modifiche ci sono» ma `git diff HEAD` è vuoto. Fix: riapplicare l'Edit sul branch e committare immediatamente.
- [2026-07-17] ⭐ PERMANENTE (Nicola: «impara questa lezione e ricordatela per sempre») **Il file `.env` del worker è `cervello/vps/.env` — NON `/opt/mycity/ad-mycity/.env` (verificato 17/7 su mycity-worker-chat.service):** il file systemd ha `EnvironmentFile=/opt/mycity/ad-mycity/cervello/vps/.env`. Il file root `.env` è vuoto/assente. Ogni nuova variabile d'ambiente (token, chiave API) va in `cervello/vps/.env` + poi `sudo systemctl restart mycity-worker-chat.service`. MAI mandare Nicola a modificare `/opt/mycity/ad-mycity/.env` — è il file sbagliato. MAI cercarlo nel codice del worker — è una variabile d'ambiente del processo.

